services:
  pinadasun-web:
    container_name: pinadasun-web
    build:
      context: .
      dockerfile: Dockerfile
    image: pinadasun-web:latest
    restart: unless-stopped
    ports:
      # Puerto 8004 expuesto en el host para el Reverse Proxy de Synology DSM
      - "8004:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:3000/api/health > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
